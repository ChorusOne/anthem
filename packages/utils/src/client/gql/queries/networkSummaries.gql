query networkSummaries($fiat: String!){
    networkSummaries(fiat: $fiat){
        name
        tokenPrice
        lastDayChange
        marketCapitalization
        expectedReward
        inflation
        supportsLedger
    }
}